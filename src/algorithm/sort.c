#include "../../includes/push_swap.h"

int	sort_10(t_list **a, t_list **b)
{
	if (!a || !b || !(*a)
		|| !(*a)->next || !(*b)
		|| !(*b)->next)
		return (1);
	pb(a, b);
	pb(a, b);
	if ((*a)->value >)
}